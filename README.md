# Wumpus World: Knowledge-Based Agent

![alt text](https://img.shields.io/badge/Python-3.13-blue?logo=python) ![alt text](https://img.shields.io/badge/Library-SymPy-green) ![alt text](https://img.shields.io/badge/GUI-Tkinter-orange) ![alt text](https://img.shields.io/badge/Package_Manager-uv-purple)

Реализация классической задачи искусственного интеллекта «Мир Вампуса» (Wumpus World). Агент исследует пещеру, избегает ловушек и охотится за золотом, используя пропозициональную логику и метод резолюций для принятия решений.

## 📸 Скриншоты

### 📜 Начало игры

Выбираем начальную конфигурацию. Размеры карты и шанс появления ям(от 0 до 1) 

![Игровой процесс](screenshot/main_menu.png)

### 🎮 Игровой процесс

Агент исследует карту, открывая «туман войны». Интерфейс отображает показания сенсоров (Ветер, Вонь).

![Игровой процесс](screenshot/gameplay_placeholder.png)

### 🏆 Победа 

Как только золото найдено, агент празднует победу.

![Экран победы](screenshot/victory_placeholder.png)

## 🧠 Как это работает?

В основе агента лежит Символьный ИИ (Symbolic AI). В отличие от нейросетей, которые оперируют вероятностями, этот агент использует строгую математическую логику для принятия решений.

1. **База Знаний (Knowledge Base)**
   Агент воспринимает мир через сенсоры и конвертирует ощущения в булевы формулы, используя библиотеку SymPy.
   - Факты: `~Pit_0_0` (в (0,0) чисто), `Wind_0_1` (в (0,1) дует).
   - Аксиомы мира: `Wind_x_y <=> (Pit_neighbor_1 | Pit_neighbor_2 | ...)`
2. **Двойной Логический Вывод (Inference)**
   Агент использует SAT-solver и метод доказательства от противного (Proof by Refutation) в двух направлениях:
   - 🛡 Доказательство Безопасности: гипотеза «Допустим, в клетке X есть яма». Если это противоречит наблюдениям (например, рядом нет ветра) — клетка помечается как SAFE.
   - ☠️ Дедукция Угроз («Эврика!»): гипотеза «Допустим, в клетке X НЕТ ямы». Если это создает противоречие (например, ветер есть, а других свободных соседей нет) — значит, в клетке гарантированно есть яма. Агент запоминает это как «стену смерти».
3. **Стратегия Исследования (Decision Making)**
   Поведение агента регулируется приоритетами:
   - *Exploration*: идти в новую доказанно безопасную клетку.
   - *Backtracking*: если новых нет, идти в старую безопасную клетку.
   - *Panic Mode* (Risk Management): агент имеет динамический «предел терпения», зависящий от размера изученной карты. Если агент застрял в локальном минимуме (ходит кругами), он игнорирует риск и делает вынужденный шаг в неизвестность, чтобы открыть новые территории.
4. **Генерация Мира (Rejection Sampling)**
   Мир генерируется процедурно, но с гарантией проходимости: создается случайная карта с препятствиями. Затем запускается алгоритм BFS (поиск в ширину), который проверяет наличие безопасного пути от старта до золота. Если пути нет — карта отбрасывается и генерируется новая, чтобы агент не оказывался заперт на старте.

## 🚀 Установка и запуск

Проект использует современный менеджер пакетов `uv`.
   - Если у вас он не установлен, его можно скачать и установить отсюда:
   - 👉 https://docs.astral.sh/uv/getting-started/installation/

### Вариант 1. Запуск из исходников

```bash
git clone https://github.com/Toksyanich/WumpusWorld.git
cd WumpusWorld
uv sync
uv run python grafics.py
```

### Вариант 2. Готовый .exe (Windows)
S
1. Скачайте архив из раздела Releases.
2. Распакуйте и запустите `WumpusWorld.exe`.
3. Python устанавливать не нужно.

4. Если вы хотите сами собрать `exe`. `.exe` будет лежать в папке `dist`.

```bash
uv run pyinstaller --noconsole --onefile --add-data "icons;icons" --name WumpusGame grafics.py
```


## 🎛 Управление

- `Авто-игра`: агент играет самостоятельно.
- `Сделать шаг`: ручной режим для отладки и демонстрации логики.
- `Пауза`: остановка и возобновление авто-игры.
- `Скорость (<< >>)`: задержка между ходами от 50 мс до 2000 мс.
- `Рестарт`: генерация нового мира.

## 🗂 Структура проекта

- `main.py` — ядро. Содержит классы `Agent` (логика на SymPy) и `WumpusWorld` (генерация мира).
- `grafics.py` — интерфейс (Tkinter), отвечает за отрисовку, анимацию и управление.
- `icons/` — набор спрайтов.

## 👤 Автор

Михаил  
Проект выполнен в рамках курса «Системы искусственного интеллекта».

